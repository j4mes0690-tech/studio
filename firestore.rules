
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Basic rules for the internal system.
    // In a production app, you would restrict these further.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
