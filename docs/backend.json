
{
  "entities": {
    "DistributionUser": {
      "title": "DistributionUser",
      "description": "A user profile with system permissions. The document ID should be the user's email or UID.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "permissions": { "$ref": "#/entities/UserPermissions" }
      },
      "required": ["name", "email"]
    },
    "UserPermissions": {
      "title": "UserPermissions",
      "description": "Access control flags for various system modules.",
      "type": "object",
      "properties": {
        "canManageUsers": { "type": "boolean" },
        "canManageSubcontractors": { "type": "boolean" },
        "canManageProjects": { "type": "boolean" },
        "canManageChecklists": { "type": "boolean" }
      }
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/users/{userEmail}": {
      "schema": "DistributionUser",
      "description": "User profiles and permissions, keyed by email for easy lookup in distribution lists."
    }
  }
}
